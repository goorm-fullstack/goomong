namespace: default
resources:
  - nginx.yaml
  - mariadb.yaml
  - backend.yaml
  - frontend.yaml
  - secret.yaml
configMapGenerator:
  - name: nginx
    files:
      - config/default.conf
  - name: init-db
    files:
      - config/init.sql